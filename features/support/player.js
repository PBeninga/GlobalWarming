const { Given, When, Then } = require('cucumber');
const assert = require('assert');

Given('A player pool', function() {
   //this.makePool();
   return 'pending';
});

When('A player opens the game', function() {
   return 'pending';
});

Then('the player is added to the pool', function() {
   //this.addPlayerToPool();
   return 'pending';
});
